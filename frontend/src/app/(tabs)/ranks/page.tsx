import { ComingSoon } from '../coming-soon';

export default function RanksPage() {
  return (
    <ComingSoon
      title="Ranks is on the way"
      description="Your Bodyrank, the Bodygraph, leagues and every badge in the game. The engine already scores your lifts — this is the screen that shows it."
    />
  );
}
